name: eskema
description: Small & composable runtime validation library
version: 0.1.0
homepage: https://github.com/nombrekeff/eskema

environment:
  sdk: '^3.0.0'

dependencies:
  collection: ^1.19.1

dev_dependencies:
  test: ^1.26.3
  lints: ^6.0.0
  