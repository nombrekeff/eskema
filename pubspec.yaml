name: json_scheme
description: Allows you to validate dynamic data with a simple yet powerful API.
version: 0.0.1
homepage: https://github.com/nombrekeff/json_scheme

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
